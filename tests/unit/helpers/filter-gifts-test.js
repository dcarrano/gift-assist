import { filterGifts } from 'gift-assist/helpers/filter-gifts';
import { module, test } from 'qunit';

module('Unit | Helper | filter gifts');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = filterGifts([42]);
  assert.ok(result);
});
